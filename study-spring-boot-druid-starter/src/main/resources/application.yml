spring:
  datasource:
    druid:
      url: jdbc:mysql://localhost:3306/study?characterEncoding=utf-8&serverTimeZone=Asia/Shanghai
      username: root
      password: password
      initialSize: 4
      minIdle: 4
      maxActive: 30
      statViewServlet:
        loginPassword: testAdmin
